version: '3.8'

services:
  chromadb:
    image: chromadb/chroma:latest
    container_name: lockerkorea-chromadb
    ports:
      - "8000:8000"
    volumes:
      - chroma-data:/chroma/chroma
    environment:
      - IS_PERSISTENT=TRUE
      - ANONYMIZED_TELEMETRY=FALSE
      - ALLOW_RESET=TRUE
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/heartbeat"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - lockerkorea-network

volumes:
  chroma-data:
    driver: local

networks:
  lockerkorea-network:
    driver: bridge

